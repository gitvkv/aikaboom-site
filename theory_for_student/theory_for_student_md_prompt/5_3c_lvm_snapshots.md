Please generate a Markdown (.md) file for the below topic in our NVIDIA-Certified Associate: AI Infrastructure and Operations
for new engineers who have limited exposure to this field.
Keep it simple to understand for a new engineer who is trying to learn AI Infrastructure and Operations.

Topic:
- 5.3c Snapshots: point-in-time copies for backup before model updates

Use the following structural requirements:

Title Format: 
# 5.3c Snapshots: point-in-time copies for backup before model updates

TAG Format:
#### 🏷️ The Operating System Layer — Linux for AI Infrastructure Operators > 5 Storage & Resource Management for AI Workloads > 5.3 Logical Volume Management (LVM) — Flexible Storage Pools

Layout & Style:
- Start with a brief context introduction.
- Use clean Markdown styling with explicit section dividers (---).
- Use descriptive emojis for every subsection header (e.g., ⚙️, 📊, 🛠️, 🕵️).
- Use bullet points and a comparison table if applicable to break down concepts clearly.

CRITICAL COMMAND RULE:
Do not show any commands, syntax, or terminal outputs under markdown code blocks or
code sections (do not use triple backticks for content). Instead, format all commands,
script examples, and expected outputs as bolded text or clean list items directly
inline within the Markdown flow so it matches our established styling perfectly.

Do not use the specific reference to "infrastructure engineers" — replace it with
the broader term "engineers" to keep the scope general.

CODE FORMATTING RULE:
If any code or commands are involved in the content, use the below guidelines:
- ALL code/command examples must be placed inside a fenced code block
  (triple backticks with a "For reference" label).
- Each statement must be on its own line — never compress multiple statements into one line.
- Output must be shown below the code block as bolded inline text prefixed
  with 📤 Output: if applicable.
- Do NOT use bold inline text for code — only for output values.

CONFLICT RESOLUTION RULE:
- Use fenced code blocks ONLY for actual code or scripts (e.g., Python, Bash).
- NEVER use fenced code blocks for plain commands, terminal outputs, or
  conceptual examples — use bolded inline text for those instead.